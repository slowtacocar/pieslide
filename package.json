{
  "name": "@pieslide/pieslide",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix dashboard && npm run build --prefix slideshow"
  },
  "dependencies": {
    "@pieslide/dashboard": "file:dashboard",
    "@pieslide/slideshow": "file:slideshow"
  }
}
